export type initStateType = {

}

const initState = {
}

export const passwordReducer = (state: initStateType = initState, action: any): initStateType => {
    switch (action.type) {
        default:
            return state
    }
}